# said-build

## Agent skills

### Issue tracker

Issues live in GitHub Issues for `Qonsult1001/said-build` (via the `gh` CLI). External PRs are also a triage surface. See `docs/agents/issue-tracker.md`.

### Triage labels

Default vocabulary: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Multi-context layout (`CONTEXT-MAP.md` at root → per-context `CONTEXT.md`). See `docs/agents/domain.md`.
