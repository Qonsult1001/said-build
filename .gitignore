/target
**/target
node_modules
*.pdb

# Downloaded CI artifacts
/dist-binaries/
/dist/

# Editor / tooling
/.vscode/
/.claude/

# Old reference material — kept locally for lookup, NOT pushed.
# (old vault-rust prototype, bench corpora, etc.)
/reference/

# Local brain files + vault DB — kept locally for testing, NOT pushed.
*.said
/vault.db

# Python build artifacts (the sca-core python binding is built on demand)
__pycache__/
*.pyc
*.pyd
*.so
# Learning factory — SPLIT OUT into its own repo:
#   https://github.com/Qonsult1001/said-learning-factory
# said-build has no build dependency on it (skill packs are discovered at runtime from
# a folder). A local working copy may remain on disk but is no longer tracked here.
learning-factory/

# Local test binaries copied to repo root for manual walkthrough testing
/said.exe
/said
/said-mcp.exe
/said-mcp

# Local Claude settings (contains secrets — never commit)
.claude/settings.local.json
/model-eval/
/research/

# LLM connector secret — provider API key, never committed
**/OrchestrationFactory.API/data/llm-config.json

# transient benchmark working dir (moat scratch — regenerated per run)
hard-eval/repo/

# MCP binary backups — never commit (repo bloat; not release assets)
production/brain/mcp/old/**/*.exe
production/brain/**/*.exe
