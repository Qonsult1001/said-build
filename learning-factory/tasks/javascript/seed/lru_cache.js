// LRUCache(capacity): O(1) get/put. get(key)->value or -1; counts as a use.
// put(key,val): insert/update (update counts as a use); evict least-recently-used over capacity.
// TODO: implement (stub throws so the gate is red).
class LRUCache {
  constructor(capacity) { throw new Error('not implemented'); }
  get(key) { throw new Error('not implemented'); }
  put(key, value) { throw new Error('not implemented'); }
}
module.exports = { LRUCache };
